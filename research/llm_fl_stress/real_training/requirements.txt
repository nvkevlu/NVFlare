# Install the repository itself separately with: pip install -e /path/to/NVFlare
# PyTorch cu126 is compatible with the cluster's CUDA 12.x driver family.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.0+cu126
transformers==4.57.6
accelerate==1.12.0
safetensors==0.7.0
sentencepiece==0.2.1
